# Phase 4D — Rusinga Layout Fix

This update replaces the earlier Rusinga map treatment with a stable responsive layout.

- No iframe map
- No absolute-positioned map content
- Map is an inline SVG that stays inside its card
- Two KWEPU-owned photos are displayed in separate responsive cards
- Text and map stack cleanly on smaller screens
- Volunteer accommodation capacity remains up to 12 volunteers
- Existing SEO and Google Search Console verification files are preserved
